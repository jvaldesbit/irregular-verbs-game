server {
    server_name verbs.jcvb.com.co;

    location / {
        proxy_pass http://localhost:4322;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}
